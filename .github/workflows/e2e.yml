name: E2E Tests

on:
    push:
        branches: [main]
    pull_request:

permissions:
    contents: read

jobs:
    e2e:
        uses: apermo/reusable-workflows/.github/workflows/reusable-wp-e2e.yml@fix/skipped-matrix-names
        with:
            wp-versions: '["latest"]'
            use-ddev: true
